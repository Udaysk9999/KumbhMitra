# Config Directory

This directory is reserved for application configuration modules such as:
- `env.config.js`: Validated environment variables and constants.
- `db.config.js`: MongoDB connection initialization using Mongoose.
- `maps.config.js`: Google Maps Platform client setup and credentials.
- `ai.config.js`: AI provider client initialization.
