# Hooks Directory

This directory will hold custom React hooks:
- `useGeolocation.js`: User location tracking with permission handling.
- `usePlaces.js`: Place query and filter management.
- `useItinerary.js`: State management for multi-stop itinerary plans.
- `useMapController.js`: Controls for camera, layers, and zoom levels.
